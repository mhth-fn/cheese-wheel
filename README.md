# 🧀 Сырное Колесо Фильмов

Веб-приложение для совместного выбора фильмов с помощью колеса фортуны.

## Установка и запуск

### 1. Скопируй папку на сервер

```bash
scp -r cheese-wheel/ user@your-server:/path/to/app/
```

### 2. Установи зависимости

```bash
cd cheese-wheel
npm install
```

### 3. Запусти сервер

**Обычный запуск:**
```bash
npm start
```

**Фоновый режим:**
```bash
node server.js &
```

**Через PM2 (рекомендуется для продакшена):**
```bash
# Установка PM2 (один раз)
npm install -g pm2

# Запуск
pm2 start server.js --name cheese-wheel

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save
```

### 4. Открой в браузере

```
http://твой-сервер:3000
```

## Настройка порта

По умолчанию сервер запускается на порту 3000. Чтобы изменить:

```bash
PORT=8080 npm start
```

Или в PM2:
```bash
PORT=8080 pm2 start server.js --name cheese-wheel
```

## Структура файлов

```
cheese-wheel/
├── package.json          # Зависимости
├── server.js             # Бэкенд (Express + SQLite + Socket.IO)
├── public/
│   ├── index.html        # HTML-разметка
│   ├── css/
│   │   ├── base.css          # CSS-переменные, reset, общие стили
│   │   ├── theme-newyear.css # Новогодняя тема (снег, гирлянды)
│   │   ├── theme-spring.css  # Весенняя тема (лепестки)
│   │   ├── auth.css          # Страница авторизации
│   │   ├── nav.css           # Навигация
│   │   ├── wheel.css         # Колесо фортуны
│   │   ├── movies.css        # Форма добавления фильмов
│   │   ├── watched.css       # Таблица просмотренных
│   │   ├── stats.css         # Статистика и поиск
│   │   ├── modal.css         # Модальные окна и админ-панель
│   │   ├── toast.css         # Уведомления
│   │   ├── connection.css    # Индикатор подключения
│   │   └── responsive.css    # Адаптив (мобильные)
│   └── js/
│       ├── app.js            # Точка входа
│       ├── state.js          # Общее состояние приложения
│       ├── api.js            # Все запросы к серверу
│       ├── socket.js         # Socket.IO (реалтайм-синхронизация)
│       ├── auth.js           # Авторизация и логин/логаут
│       ├── router.js         # Клиентская навигация
│       ├── nav.js            # Рендеринг навигации
│       ├── wheel.js          # Canvas-колесо и список фильмов
│       ├── spin.js           # Анимация вращения и результат
│       ├── audio.js          # Звуки (Web Audio API)
│       ├── watched.js        # Таблица просмотренных
│       ├── ratings.js        # Рейтинги и сортировка
│       ├── stats.js          # Панель статистики
│       ├── theme.js          # Темы и декорации
│       ├── events.js         # Привязка DOM-событий
│       └── utils.js          # Утилиты (toast, escapeHtml, formatDate)
└── cheese_wheel.db       # База данных (создаётся автоматически)
```

## Функции

- **Авторизация** — выбор пользователя (Антон, Сергей, Пётр, Митя) + гостевой режим
- **Колесо фортуны** — добавление фильмов, вращение с анимацией и звуком, автоматический перенос в просмотренные
- **Просмотренные** — таблица с оценками от каждого пользователя, сортировка, поиск, статистика
- **Темы** — три визуальных темы (сырная, новогодняя, весенняя) с анимированными декорациями
- **Реалтайм** — синхронизация между вкладками через Socket.IO

## Бэкап базы данных

База хранится в файле `cheese_wheel.db`. Для бэкапа просто скопируй этот файл.

```bash
cp cheese_wheel.db cheese_wheel_backup_$(date +%Y%m%d).db
```

---

Приятного просмотра! 🧀🎬
